{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "gunicorn core.wsgi:application --bind 0.0.0.0:8000 --workers 2 --timeout 300",
    "restartPolicyType": "ON_FAILURE"
  }
}
